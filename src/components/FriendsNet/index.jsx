import UsersList from "../UsersList";

import { Section } from "./styles";

const FriendsNet = () => {
  return (
    <Section>
      <UsersList type="socialnet" />
    </Section>
  );
};

export default FriendsNet;
